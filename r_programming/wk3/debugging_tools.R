# useful functions for debugging
  
traceback() #prints function call stack after error occurs

debug() #flags function for debug so you can check line-by-line

browser() #suspends execution of function and puts function in debug mode

trace() # can put debug code into function at specific places

recover() #allows modification of error behavior


